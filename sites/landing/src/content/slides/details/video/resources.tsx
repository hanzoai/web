import React from 'react'
import { type LucideProps } from 'lucide-react'

const Resources: React.FC<LucideProps> = (props: LucideProps) => (
    <source src='/assets/hanzo-site-animation/resources.mp4' type='video/mp4'></source>
)

export default Resources