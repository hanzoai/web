import React from 'react'
import { type LucideProps } from 'lucide-react'

const Product: React.FC<LucideProps> = (props: LucideProps) => (
    <source src='/assets/hanzo-site-animation/product.mp4' type='video/mp4'></source>
)

export default Product