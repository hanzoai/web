// src/index.coffee
var findCoffee;
var version;

version = function(coffee) {
  return parseInt((coffee.VERSION.split('.'))[0], 10);
};

var index = findCoffee = function(wanted) {
  var coffee, err, i, len, pkg, ref;
  if (wanted == null) {
    wanted = 1;
  }
  ref = ['coffeescript', 'coffee-script'];
  for (i = 0, len = ref.length; i < len; i++) {
    pkg = ref[i];
    try {
      coffee = require(pkg);
      if ((version(coffee)) === wanted) {
        return coffee;
      }
    } catch (error) {
      err = error;
    }
  }
  throw new Error('Unable to find CoffeeScript matching version ' + wanted);
};

export default index;
//# sourceMappingURL=find-coffee.mjs.map
