import chai from 'sake-chai';
import mocha from 'sake-mocha';

// src/index.coffee
var index = function(opts) {
  if (opts == null) {
    opts = {};
  }
  chai(opts);
  return mocha(opts);
};

export default index;
//# sourceMappingURL=sake-test.mjs.map
