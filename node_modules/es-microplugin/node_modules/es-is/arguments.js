// Generated by CoffeeScript 1.12.5
var isArguments;

import toString from 'es-tostring';

import isArrayLike from './array-like';

import isArray from './array';

import isFunction from './function';

import isObject from './object';

export default isArguments = function(value) {
  var isOldArguments, isStandardArguments;
  isStandardArguments = toString(value) === '[object Arguments]';
  isOldArguments = !isArray(value) && isArrayLike(value) && isObject(value) && isFunction(value.callee);
  return isStandardArguments || isOldArguments;
};

//# sourceMappingURL=arguments.js.map
