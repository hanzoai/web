// Generated by CoffeeScript 1.12.5
var isArrayLike;

import hasOwn from 'es-hasown';

import isBool from './bool';

import isInfinite from './infinite';

import isNumber from './number';

export default isArrayLike = function(value) {
  return !!value && !isBool(value) && hasOwn(value, 'length') && isFinite(value.length) && isNumber(value.length) && value.length >= 0;
};

//# sourceMappingURL=array-like.js.map
