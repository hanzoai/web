// Generated by CoffeeScript 1.12.5
var isWithin;

import isInfinite from './infinite';

import isNumber from './number';

import {
  isActualNaN
} from './utils';

export default isWithin = function(value, start, finish) {
  var isAnyInfinite;
  if (isActualNaN(value) || isActualNaN(start) || isActualNaN(finish)) {
    throw new TypeError('NaN is not a valid value');
  } else if (!isNumber(value) || !isNumber(start) || !isNumber(finish)) {
    throw new TypeError('all arguments must be numbers');
  }
  isAnyInfinite = isInfinite(value) || isInfinite(start) || isInfinite(finish);
  return isAnyInfinite || value >= start && value <= finish;
};

//# sourceMappingURL=within.js.map
