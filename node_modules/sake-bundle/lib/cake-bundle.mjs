// src/index.coffee
var bundle;
var handroll;
var path;

handroll = require('handroll');

path = require('path');

bundle = function(opts) {
  return handroll.bundle(opts);
};

bundle.write = function(opts) {
  return handroll.write(opts);
};

var index = function(opts) {
  if (opts == null) {
    opts = {};
  }
  global.bundle = bundle;
  global.Bundle = handroll.Bundle;
  if (opts.entry != null) {
    return task('bundle', 'bundle javascript', function() {
      return bundle.write(opts);
    });
  }
};

export default index;
//# sourceMappingURL=cake-bundle.mjs.map
