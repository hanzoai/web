'use strict';

// src/index.coffee
var bundle;
var handroll;

handroll = function() {
  return require('handroll');
};

bundle = function(opts) {
  return handroll().bundle(opts);
};

bundle.write = function(opts) {
  return handroll().write(opts);
};

var index = function(opts) {
  if (opts == null) {
    opts = {};
  }
  if (opts.global !== false) {
    global.bundle = bundle;
    Object.defineProperty(global, 'Bundle', {
      get: function() {
        return handroll().Bundle;
      },
      enumerable: true
    });
  }
  if (opts.entry != null) {
    return task('bundle', 'bundle javascript', function() {
      return bundle.write(opts);
    });
  }
};

module.exports = index;
//# sourceMappingURL=sake-bundle.js.map
